# Real Supabase project config (URL + public anon key) -- never commit.
# Copy supabase-config.example.js to supabase-config.js and fill in your
# own project's values. See docs/auth_setup.md.
supabase-config.js

# Env files (kept in case a future build step is added; never commit)
.env
.env.local
.env.*.local

# OS / editor cruft
.DS_Store

# React / Vite build artifacts and dependencies
node_modules/
dist/
dist-ssr/
*.local
.vite/

# Test / tooling output
coverage/
playwright-report/
test-results/
.eslintcache

# Internal migration working notes (not shipped)
migration-notes/

# Generated at build/dev time from legacy/report.html (10MB) — not committed
public/report-body.html
